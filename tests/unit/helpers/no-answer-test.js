import { noAnswer } from 'question-answer/helpers/no-answer';
import { module, test } from 'qunit';

module('Unit | Helper | no answer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = noAnswer([42]);
  assert.ok(result);
});
